services:
  tracker-dashboard:
    image: ghcr.io/aerya/tracker-dashboard:latest
    container_name: tracker-dashboard
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config
